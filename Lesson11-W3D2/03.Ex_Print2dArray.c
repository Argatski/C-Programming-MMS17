#include <inttypes.h>
#include <stdio.h>

void print_row(int (*arr)[4]) {
  for (int i = 0; i < 4; i++) {
    printf("%d ", (*arr)[i]);
  }
  printf("\n");
}

void print_row2(int *p) 
{
    for (size_t i =0; condition; inc-expression) {
    
    }
}